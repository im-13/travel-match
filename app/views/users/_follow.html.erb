<%= form_for(Follows.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.uuid %></div>
  <div class="col-sm-4"> 
  	<%= f.submit "Follow", class: "btn btn-success" %>
  </div>
<% end %>