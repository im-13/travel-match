<% provide(:title, 'All users') %>
<div class="container">
	<h1>All users</h1>

	<div class="container">
	<%= will_paginate @users %>

	<ul class="users">
	  <%= render @users %>
	</ul>

	<%= will_paginate @users %>
</div>

