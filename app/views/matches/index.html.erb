<% provide(:title, 'Your TravelMatch') %>
<h1>Your TravelMatch</h1>

<div class="container">
	<%= will_paginate @matches %>

	<ul class="users">
	  <%= render @matches %>
	</ul>

	<%= will_paginate @matches %>
</div>

