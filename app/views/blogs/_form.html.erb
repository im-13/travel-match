<div class="container">
  <%= simple_form_for(@blog, html: { class: 'form-horizontal' }) do |f| %>
    <% if @blog.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

        <ul>
        <% @blog.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    
    <!--%= f.input :email, collection: User.all, label_mathod: full_name %-->
    <%= f.input :content, input_html: {rows: 4, class: 'input-xxlarge'} %>
    <!--%= f.field_for :carrierwave_image do |carrierwave_image_field|%-->

      

    <% end %>

    <div class="form-actions">
      <%= f.button :submit, class: "btn btn-primary" %>
    </div>
  <% end %>
</div>