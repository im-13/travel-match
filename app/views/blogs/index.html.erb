<% provide(:title, 'Travel Blog') %>
<div class="container">
  <div class="page-helper">
    <h1>Travel Blog</h1>
  </div>

  <div class="container">
    <%= link_to 'Write a New Post', new_blog_path, class: "btn btn-primary" %>
    <br>
    <%= will_paginate @blogs %>
    <ul class="user">
      <%= render @blogs %>
    </ul>
    <%= will_paginate @blogs %>
  </div>
</div>

