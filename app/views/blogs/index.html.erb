<% provide(:title, 'Travel Blog') %>
<div class="container">
  <div class="page-helper">
    <h1>Travel Blog</h1>
  </div>

  <div class="container">
    <%= will_paginate @blogs %>
    <ul class="user">
      <%= render @blogs %>
    </ul>
    <%= will_paginate @blogs %>
  </div>
</div>

