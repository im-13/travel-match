<% provide(:title, 'All trips') %>
<div class="container">
  <p id="notice"><%= notice %></p>
  <div class="page-helper">
    <h1>All trips</h1>
  </div>

  <div class="container">
    <%= link_to "Do you want to add a trip?", newtrip_path %>
  </div>

  <div class="col-md-8">

      <ul class="posts">
        <%= render @trips %>
      </ul>
      <%= will_paginate @trips %>

  </div>

<!--% @trips.each do |trip| %>
  <div class="col-md-4">
    <div class="thumbnail">

      <div class="blog-container-responsive row-space-2">
        <div class="blog-content">
          <div class="panel-body">
            <div class="row">
              <% cur = User.find_by(uuid: trip.user_uuid) %>
              <div class="col-sm-3 text-center row-space-2"> 
                <a class="media-photo media-round row-space-1 text-center"> <img class="lazy" style-"display: inline;" >
                <% if cur.avatar? %>
                  <img class="img-circle"<%= image_tag cur.avatar.small.url %></a>
                <% else %>
                  <img class="img-circle"<%= image_tag cur.gravatar_url %></a>
                <% end %>
              
                <div class="name">
                  <a class="text-muted link-reset">
                  <p><strong><%= cur.first_name %></strong></p></a>
                </div>
              </div>
              <!--% image_tag blog.user_gravatar_url %-->
              <!--%= link_to blog.user_name, User.find(blog.user_uuid) %-->  
              <!--div class="col-sm-9">
                <div class="row-space-2">
                  <div class="expandable-content">
                    <strong> 
                    	<%= link_to trip.place + " in " + trip.country, trip %>
                    </strong>
                    <br>
                    <i>
	                    from <%= trip.date_from.to_time.strftime('%b %e, %Y') %>
	                    to <%= trip.date_to.to_time.strftime('%b %e, %Y') %>
	                </i>
	                <br>
	                <br>
	                <%= trip.description %>
                    <% if trip.photo? %>
                      <%= link_to image_tag(trip.photo.thumb.url), trip %>
                    <% end %>
                  </div>
                </div>

                <!--h2> @blog.comments.count </h2-->
                <!--%= render 'blogs/comments' %-->


                <!--div class="meta">
                  <%= link_to time_ago_in_words(trip.created_at) + " ago", trip %>
                  <% if cur.admin? || cur == current_user %>
                    | <%= link_to 'Edit', edit_trip_path(trip) %>
                    | <%= link_to 'Delete', trip, method: :delete, data: { confirm: 'Are you sure you want to delete this trip?' } %>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div> 
  </div>
<!--% end %-->
</div>

